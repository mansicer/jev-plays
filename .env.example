# TypeSafe AI (jev, System One) — required for every jev_* policy
TYPESAFE_API_KEY=
# OpenAI-compatible endpoint — required for --planner and --policy llm
OPENAI_API_KEY=
OPENAI_MODEL_NAME=
# optional: custom base URL for the OpenAI-compatible endpoint
# OPENAI_BASE_URL=
# optional: classic (default) | full
# CRAFTAX_VARIANT=classic
